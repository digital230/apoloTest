type Query {
  allUsers(all: Boolean!): defaultRes!
}

type defaultRes {
  success: Boolean!
  data: [User]!
}

type User {
  _id: ID!
  name: String
}

type result {
  success: Boolean!
  msg: String!
}


type Mutation {
  insertUser(name: String!): result!
}
